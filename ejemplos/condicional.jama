hambriento

ingrediente torta x <- 4.
ingrediente torta y <- 6.
ingrediente torta total <- x + y.

servir("El total es: ")
servir(total)

¿si? (total >= 10) {
    servir("Total es mayor o igual a 10")
} ¡de otro modo! {
    servir("Total es menor a 10")
}

satisfecho